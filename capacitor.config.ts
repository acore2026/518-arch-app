import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.acore2026.intentlink',
  appName: 'IntentLink Demo',
  webDir: 'dist',
};

export default config;
